/** Playback seconds before a listen counts on feeds (recent, others listening). */
export const LISTEN_QUALIFY_SECONDS = 30;

/** Client heartbeat interval while audio plays. */
export const LISTEN_HEARTBEAT_SECONDS = 15;

/** How often the home shelf polls for other listeners. */
export const OTHERS_LISTENING_POLL_MS = 15_000;
