# Umbrel agent guidance used for this package lives upstream:
# https://github.com/getumbrel/umbrel-apps (AGENTS.md + .claude/skills)
#
# - umbrel-develop-app: browser first-run, Docker-first, durable paths
# - umbrel-package-app: umbrel-app.yml / compose / app_proxy / unique port
# - umbrel-test-app: install through Umbrel before store PR
#
# This repository is the upstream Polarr app. The installable Umbrel shell is in
# umbrel-package/polarr/ and should be copied into umbrel-apps when ready to submit.
